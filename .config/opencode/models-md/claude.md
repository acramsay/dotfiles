# Python usage

Prefer a dedicated CLI tool over a Python script when one exists for the task.

When Python is genuinely the right tool, run it via `uv run` (or `uv` subcommands
generally) instead of invoking `python`/`python3` directly.
