# Python usage

Never invoke `python` or `python3` directly. Reach for a dedicated CLI tool first when
one exists for the task.

When Python is genuinely the right tool, run it via `uv run` (or `uv` subcommands
generally).
